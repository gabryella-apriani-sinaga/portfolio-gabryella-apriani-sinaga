import Banner from "./Banner";
import Navbar from "./Navbar";

const Hero = () => {
  return (
    <section className="hero">
      <Navbar />
      <Banner />
    </section>
  );
};

export default Hero;
